#include <stdio.h>

int main() {
   // A program to print natural number from 10 to 20 when the initial counter is initialized to be 0 \n\n.
    int n = 0 ;
    while (n<=20)
    { 
        if(n>=10){ 
            printf("%d\n",n);
        }
        n++ ;
    }
    return 0;
}
